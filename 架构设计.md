# SmartProxy 智能分流系统 — 架构设计文档

> 版本 4.0.0 · 最后更新: 2026-07-24

---

## 目录

1. [系统概述](#1-系统概述)
2. [整体架构](#2-整体架构)
3. [模块详解](#3-模块详解)
4. [路由决策流程](#4-路由决策流程)
5. [两条链路](#5-两条链路)
6. [熔断与降级](#6-熔断与降级)
7. [历史学习与自适应](#7-历史学习与自适应)
8. [配置体系](#8-配置体系)
9. [已知问题与修复历史](#9-已知问题与修复历史)

---

## 1. 系统概述

SmartProxy 是一个**内容感知的内容感知路由代理**，部署在 LLM 客户端与上游 API 之间。核心策略 **flash-first**：用便宜的模型处理大部分请求，仅在必要时升级到更强模型，在**成本**和**质量**之间取得平衡。

### 设计目标

| 目标 | 实现方式 |
|------|----------|
| 降低成本 | 简单任务走 flash（约 pro 的 1/10 价格） |
| 保障质量 | 复杂任务自动跳过 flash，直接使用强模型 |
| 故障自愈 | 断路器自动熔断 + 探活恢复 |
| 零中断升级 | kimi 配额耗尽 → 无缝降级到 deepseek pro |
| 可观测 | 结构化日志 + 可视化分析报告 |

### 与 cc-switch 的关系

SmartProxy 与 cc-switch 是**互补而非替代**关系：

- **cc-switch**：管理供应商配置，支持热切换
- **SmartProxy**：在单个供应商内部做 flash-first 分流

用户通过 cc-switch 在"智能分流"和"直连"两种模式间切换：

```
┌─────────────────────────────────────────────────────┐
│                    cc-switch                          │
│                                                       │
│  [智能分流供应商] ──→ SmartProxy(:8000) ──→ 后端     │
│  [直连供应商]     ──→ api.deepseek.com               │
│                                                       │
│  可随时热切换，SmartProxy 无需重启                     │
└─────────────────────────────────────────────────────┘
```

---

## 2. 整体架构

### 2.1 项目结构

```
.cc-switch/
│
├── start_proxy.bat           # 启动脚本
├── proxy_config.yaml         # YAML 路由配置
├── proxy_state.json          # 熔断状态持久化
├── .env                      # API Key 环境变量
├── settings.json             # cc-switch 桌面应用配置
├── analyze_routing.py        # 日志分析 & HTML 报告生成
│
├── smart_proxy/              # ★ Python 源码包
│   ├── __init__.py           #   版本声明
│   ├── __main__.py           #   入口: uvicorn.run()
│   ├── app.py                #   FastAPI 装配 + 探活循环
│   ├── server.py             #   请求 handler + 鉴权
│   ├── controller.py         #   请求编排
│   ├── router.py             #   路由决策引擎
│   ├── config.py             #   Pydantic 配置 + YAML 热加载
│   ├── state.py              #   熔断/过载 TTL 管理
│   ├── circuit.py            #   指数退避计算
│   ├── metrics.py            #   延迟追踪 + 相似度历史
│   ├── cache.py              #   LRU 响应缓存
│   └── logging_setup.py      #   结构化日志 (stdout + 文件)
│
├── logs/
│   ├── cc-switch.log         # Rust 主程序日志（不做为解析源）
│   └── smart_proxy.log       # SmartProxy 路由日志（JSON Lines）
│
└── docs/                     # 文档（可选）
    ├── 使用指南.md
    └── 设计文档.md
```

### 2.2 请求生命周期

```
Claude Code
    │  POST /v1/messages  (stream=true/false)
    ▼
cc-switch (供应商路由)
    │  根据当前 profile 决定目标 URL
    ▼
FastAPI (server.py)
    │  鉴权 → Body 大小检查 → JSON 解析
    ▼
Controller (controller.py)
    │  route() 做路由决策
    ▼
Router (router.py)
    │  关键词检测 / token 评估 / 历史参考
    │  → flash_first 或 skip_flash
    ▼
Controller 执行
    │  flash 请求 → 成功？→ 检查不足？→ 升级？
    ▼
Upstream API (deepseek/kimi)
    │  返回 HTTP 响应或错误
    ▼
Controller 处理结果
    │  → 成功响应返回客户端
    │  → 失败 → 熔断 → 降级/升级
```

---

## 3. 模块详解

### 3.1 入口层 (`__main__.py` + `app.py`)

```
python -m smart_proxy
    │
    ├── config.get_settings()        # 加载 YAML
    ├── setup_logging()             # 配置日志
    └── uvicorn.run(app, :8000)     # 启动 FastAPI
        │
        ├── lifespan：启动时
        │   ├── 配置 LatencyTracker
        │   ├── 创建 httpx.AsyncClient
        │   └── 启动后台探活协程
        │
        └── lifespan：关闭时
            ├── 取消探活
            └── 关闭 httpx 连接池
```

**后台探活 (`_probe_loop`)：** 每 30s 遍历所有熔断/过载的后端，发一条 mininal 请求（`{"messages":[{"role":"user","content":"hi"}],"max_tokens":1}`），成功则自动关闭断路器。

### 3.2 路由引擎 (`router.py`)

路由决策按优先级分 4 步，**任何一步命中即提前返回**：

```
请求进入
    │
    ├── [0] Token 长度 ≥100k？
    │      → 超长上下文，跳过 flash，直接最强模型
    │
    ├── [1] 会话粘性命中？
    │      → 同一用户会话复用上次的后端+tier
    │
    ├── [2] 相似历史 flash 失败率 >30% 且样本 ≥3？
    │      → 历史数据表明 flash 在类似问题上不行
    │
    ├── [3] 关键词命中？
    │      → 重构/安全/迁移等复杂任务
    │
    └── [4] 默认：flash_first
           → deepseek flash + 保留升级目标
```

**关键词检测实现：** `_skip_flash()` 只检查**最后一条** user 消息的文本（`_extract_user_text` 方向遍历 + 首次命中返回），避免系统 prompt 误触发。

**升级目标选择：** `_pick_best()` 实现两条链路的切换点：
```python
def _pick_best(settings):
    if "kimi" in settings.backends and _is_available("kimi"):
        return "kimi", "default"      # 链路 A
    return "deepseek", "pro"          # 链路 B
```

### 3.3 请求编排 (`controller.py`)

编排层负责**执行**路由决策，包括：

**flash_first 流程：**

```
发 flash 请求
    │
    ├── 成功？
    │     ├── 流式 → 直接转发（不评估 stop_reason）
    │     │
    │     └── 非流式
    │           ├── stop_reason = max_tokens & output <100
    │           │   → 升级（截断）
    │           ├── stop_reason = tool_use
    │           │   → 升级（工具调用）
    │           ├── 延迟 >15s
    │           │   → 升级（太慢）
    │           └── 其他 → 直接返回
    │
    └── 失败（HTTP 错误/异常）
          → 熔断 + 升级到备用后端
```

**skip_flash 流程：**

```
发 skip_flash 目标请求（kimi / pro）
    │
    ├── 成功 → 返回
    │
    └── 失败 → 熔断 + fallthrough 到 flash 作为兜底
```

**三级降级兜底：**

```
flash 升级目标失败
    → 任何可用后端（排除已试的）
        → 仍失败 → 502 All backends failed
```

### 3.4 熔断系统 (`state.py` + `circuit.py`)

**四类熔断分级：**

| 错误类型 | 触发条件 | 基础冷却 | 退避策略 |
|----------|----------|----------|----------|
| **quota** | 429 / 403 (billing) | 3600s (1h) | 指数退避 ×2 |
| **server** | 5xx / 403 (非 billing) | 300s (5min) | 指数退避 ×2 |
| **network** | 连接超时/断连 | 60s (1min) | 指数退避 ×2 |
| **permanent** | 403 + 关键词（quota/billing） | 18000s (5h) | 一次性，不退避 |

**指数退避公式：**
```python
def effective_block(config, kind, fail_count):
    base = block_seconds(config, kind)
    secs = base × (backoff_multiplier ** max(0, fail_count - 1))
    return min(secs, backoff_max_seconds)
```

示例（网络错误 fail_count 递增）：60s → 120s → 240s → 480s → ... → 上限 7200s

**Overload 标记**（独立的 429 短时保护）：
- 标记 10 分钟自动过期
- 不影响断路器状态
- 用于短时限流保护

**状态持久化：** 断路器状态写入 `proxy_state.json`，重启后仍记忆。

### 3.5 历史学习 (`metrics.py`)

**SimilarityBuffer** — 基于 Jaccard 相似度的结果缓冲：

```python
# 每条记录
{
    "text": "...",           # 请求文本（截断 500 字）
    "backend": "deepseek",
    "tier": "flash",
    "success": True/False,
    "latency_ms": 1234,
    "stop_reason": "end_turn",
}

# 新请求评估
get_stats(text) → {
    "flash_failure_rate": 0.0-1.0,   # 相似请求的 flash 失败率
    "flash_avg_latency": 1234,       # 加权平均延迟
    "flash_samples": 5,              # 相似样本数
}
```

相似度计算：将文本拆分为 3-gram 集合，计算 Jaccard 系数。只有 sim ≥0.1 才计入。

**LatencyTracker** — 滑动窗口延迟统计：

- 窗口大小：8 个样本（可配置）
- 降级判定：平均延迟 >8s 或错误率 ≥50%
- 黏性标记：降级后持续 30s，避免短时反复重试

### 3.6 响应缓存 (`cache.py`)

- LRU 淘汰，最大 128 条目
- TTL 5 分钟
- Key = SHA256(model + messages)
- **仅缓存非流式请求**
- `X-SP-Cache: hit` 响应头标识缓存命中

### 3.7 日志系统 (`logging_setup.py`)

JSON Lines 结构化日志，双写：

| 目标 | 路径 | 用途 |
|------|------|------|
| stdout | 终端 | 实时查看 |
| 文件 | `logs/smart_proxy.log` | 分析脚本消费 |

每条日志包含：
```json
{"ts": "2026-07-24T14:59:38", "level": "INFO", "msg": "...", "request_id": "abcd1234"}
```

路由事件类型：
- `Route: ... strategy=skip_flash` — 跳过 flash
- `FlashFirst: ... upgrade=...` — flash-first 策略
- `Flash inadequate (...) → upgrading to ...` — 升级
- `Flash failed (...) → upgrading to ...` — 失败升级
- `Upstream Error ...` — 上游错误
- `Cache hit for ...` — 缓存命中

---

## 4. 路由决策流程

### 4.1 完整决策树

```
HTTP POST /v1/messages
    │
    ▼
server.py: 鉴权 → Body 大小检查 → JSON 解析
    │
    ▼
controller.py: route(settings, messages)
    │
    ├── Router.token_length (>100k?)
    ├── Router.session_sticky (命中?)
    ├── Router.similarity_buffer (历史 flash 失败率 >30%?)
    ├── Router.skip_flash (关键词命中?)
    └── Router.default (flash_first)
    │
    ▼ 返回 RoutingScore {backend, tier, strategy, upgrade_backend}
    │
    ▼
Controller 执行策略
    │
    ├── skip_flash
    │   ├── 发 kimi/k2.7 (或 deepseek/pro)
    │   │   ├── 成功 → 返回
    │   │   └── 失败 → 熔断 → fallthrough 到 flash
    │   └── 发 deepseek/flash（兜底）
    │       ├── 成功 → 返回
    │       └── 失败 → 502
    │
    └── flash_first
        ├── 缓存检查 → 命中则直接返回
        │
        ├── 发 deepseek/flash
        │   ├── 成功（流式）→ StreamingResponse
        │   │
        │   ├── 成功（非流式）
        │   │   ├── 检查不足：截断/工具调用/慢
        │   │   │   ├── 判断为充足 → Response + 缓存
        │   │   │   └── 不足 → 升级
        │   │   │       ├── 发升级目标（kimi/pro）
        │   │   │       │   ├── 成功 → 返回
        │   │   │       │   └── 失败 → 最终降级
        │   │   │       └── 发兜底（任何可用后端）
        │   │   │           ├── 成功 → 返回
        │   │   │           └── 失败 → 502
        │   │   │
        │   └── 失败（HTTP 错误）
        │       └── 熔断 → 升级
        │           ├── 发升级目标
        │           │   ├── 成功 → 返回
        │           │   └── 失败 → 最终降级
        │           └── 发 deepseek/pro 兜底
        │               ├── 成功 → 返回
        │               └── 失败 → 502
        │
        └── 所有路径都失败 → 502
```

### 4.2 状态机：熔断

```
CLOSED (正常)
    │  HTTP 5xx / 403 / 429 / 网络异常
    ▼
OPEN (熔断)
    │  探活协程：每 30s 发探活请求
    │  探活成功 + 过了 min_block_seconds
    ▼
CLOSED (恢复)
```

---

## 5. 两条链路

### 链路 A: kimi 可用

```
flash_first
    │
    ├── flash 足够 → deepseek-v4-flash 结果 ✅
    │
    └── flash 不足
         → kimi 可用 → kimi/k2.7 → 结果 ✅
```

### 链路 B: kimi 不可用（额度耗尽/服务内熔断）

```
flash_first
    │
    ├── flash 足够 → deepseek-v4-flash 结果 ✅
    │
    └── flash 不足
         → kimi 不可用
         → _pick_best() 识别 kimi blocked
         → deepseek-v4-pro → 结果 ✅
```

**切换条件在代码中动态判断（`_pick_best`），无需配置：**

```python
def _pick_best(settings):
    if "kimi" in settings.backends and _is_available("kimi"):
        return "kimi", "default"      # 链路 A
    return "deepseek", "pro"          # 链路 B
```

---

## 6. 熔断与降级

### 6.1 熔断触发层级

```
HTTP 响应
    │
    ├── 4xx 客户端错误
    │   ├── 401/403 (billing keywords) → permanent 5h
    │   ├── 403 (其他) → server 5min
    │   └── 429 → overload + quota 1h
    │
    ├── 5xx 服务端错误
    │   └── server 5min（指数退避）
    │
    └── 网络/超时异常 (status_code=0)
        └── network 1min（指数退避）
```

### 6.2 降级路径

```
请求失败
    │
    ├── flash 失败 → 升级到 _pick_best()
    │   ├── kimi → 再失败 → 最终降级（任何可用后端）
    │   └── pro  → 再失败 → 502
    │
    └── skip_flash 目标失败
        → 熔断 → fallthrough 到 flash
        → flash 再失败 → 502
```

---

## 7. 历史学习与自适应

### 7.1 SimilarityBuffer 的工作方式

```
请求 A 的文本 "帮我重构这段代码"
    │  3-gram: {"帮我重","我重构","重构这","构这段","这段代","段代码"}
    ▼
存入 buffer: {text, backend, tier, success, latency_ms, stop_reason}
    │
    ▼ 后续收到 "帮我重构这个函数"
    │  3-gram: {"帮我重","我重构","重构这","构这个","这个函","个函数"}
    │  Jaccard 与历史 A 的相似度 = 交集 3 / 并集 9 = 0.33
    ▼
get_stats() 返回 flash_failure_rate 加权统计
    │  如果 >30% → 跳过 flash
    ▼
路由决策
```

### 7.2 自适应效果

| 场景 | 初始行为 | 学习后行为 |
|------|----------|-----------|
| 用户频繁发重构类问题 | flash_first → 每次失败 → 升级 | 跳过 flash → 直接 pro |
| deepseek flash 突然变慢 | flash_first → 每次超时 | 跳过 flash → 用升级目标 |
| 新类型问题 | flash_first → 试探 | 积累样本后调整 |

---

## 8. 配置体系

### 8.1 配置层次

```
环境变量 (.env)           API Key、代理设置
    │
YAML 配置 (proxy_config.yaml)    路由策略、熔断参数
    │
Pydantic 模型 (config.py)   运行时验证 + 热加载
    │
运行时状态 (state.py)     熔断/过载 TTL 管理
```

### 8.2 配置热加载

`get_settings()` 每次调用检查 `proxy_config.yaml` 的 `st_mtime`，文件变更自动重加载：

```python
def get_settings() -> Settings:
    m = cfg_path.stat().st_mtime
    with _lock:
        if _cached[0] is None or m != _mtime[0]:
            _cached[0] = load_settings()
            _mtime[0] = m
        return _cached[0]
```

**注意：** `backends` 配置修改后需重启 SmartProxy。

### 8.3 proxy_state.json 格式

```json
{
  "_version": 2,
  "circuit": {
    "kimi": {
      "state": "OPEN",
      "blocked_until": 1784893583.313,
      "blocked_at": 1784875583.313,
      "fail_count": 3
    }
  },
  "overload": {
    "kimi": {
      "marked_at": 1784875583.000
    }
  }
}
```

向后兼容：`{name}_blocked_until` 旧格式仍可识别。

---

## 9. 已知问题与修复历史

### 9.1 已修复 Bug

| Bug | 模块 | 症状 | 修复 |
|-----|------|------|------|
| **系统 prompt 误判跳过 flash** | `router.py` | 所有请求均 `skip_flash` | `_extract_user_text` 改为只取最后一条 user 消息 |
| **_handle_error 参数签名错误** | `controller.py` L269 | `_handle_error(..., None, settings)` — 多传了一个 None 参数 | 删除多余的 `None` |
| **_return_response 调用参数错位** | `controller.py` L330 | `_return_response(resp, is_stream, model, messages, settings)` — 缺少 `latency_ms` 和 `backend` | 补全所有 7 个参数 |
| **端口检查死循环** | `start_proxy.bat` | taskkill 失败后无限 `goto check_port` | 改为单次检测 + 告警跳过 |

### 9.2 设计约束 / 已知局限

| 局限 | 说明 |
|------|------|
| **流式不检查 stop_reason** | SSE 流无法在转发前读完整响应，所以流式请求永远不触发升级。依赖 SimilarityBuffer 历史数据在请求前做判断 |
| **只能添加 deepseek + kimi** | 代码中 `backend["deepseek"]` 硬编码为 flash_first 的后端 |
| **config.py 内存缓存** | 简单 mtime 检查，不支持监听文件事件 |
| **analyze_routing.py 只读 cc-switch INFO 日志** | 不解析 DEBUG/ERROR 级别，也不被动监听 |



### 9.3 未来可能的改进方向

- 支持流式响应评估（需缓冲前半段 + 流送后半段）
- 支持自定义 flash 后端（不硬编码 deepseek）
- 支持文件系统事件监听代替 mtime 轮询
- 日志分析脚本支持实时监控模式
- 支持更多降级策略（重试、负载均衡）
