# SmartProxy 智能分流系统 — 架构设计文档

> 版本 4.0.0 · 最后更新: 2026-07-24

---

## 1. 系统概述

SmartProxy 是一个**内容感知的 LLM API 请求分流代理**，部署在 cc-switch 与上游 LLM 提供商之间。核心策略是 **flash-first**：简单任务用便宜的 flash 模型，复杂任务或 flash 不足以升级到更强的模型。

```
┌─────────────┐     ┌─────────────┐     ┌──────────────────┐
│  Claude Code │ ──→ │  cc-switch   │ ──→ │  SmartProxy       │
│  (用户客户端) │     │  (供应商管理) │     │  (内容感知分流器)  │
└─────────────┘     └─────────────┘     └───────┬──────────────┘
                                                │
                    ┌───────────────────────────┼──────────────┐
                    ▼                           ▼              ▼
              ┌──────────┐              ┌──────────┐    ┌──────────┐
              │ DeepSeek │              │   Kimi   │    │ OpenAI…  │
              │ flash/pro│              │   K2.7   │    │ (预留)   │
              └──────────┘              └──────────┘    └──────────┘
```

### 1.1 双路由模式

用户通过 cc-switch 在两种模式间热切换：

| 模式 | 供应商 ID | 请求路径 | 适用场景 |
|------|-----------|----------|----------|
| **智能分流** | `universal-claude-...` | Claude → cc-switch → **SmartProxy(:8000)** → 后端 | flash-first 分流 |
| **直连模式** | `d22086d1-...` | Claude → cc-switch → **api.deepseek.com** | 需要稳定输出，跳过分流层 |

---

## 2. 项目结构

```
.cc-switch/
├── start_proxy.bat          # 启动脚本（端口检查 + 拉起 uvicorn）
├── proxy_config.yaml        # YAML 路由配置（后端、策略、熔断参数）
├── proxy_state.json         # 熔断/过载状态持久化文件
├── settings.json            # cc-switch 桌面应用配置
├── .env                     # API Key 环境变量
├── analyze_routing.py       # 日志分析 & 可视化报告生成脚本
│
├── smart_proxy/             # ★ 核心分流代理 Python 包
│   ├── __init__.py          # 版本声明 (v4.0.0)
│   ├── __main__.py          # 入口: uvicorn.run()
│   ├── app.py               # FastAPI 装配 + 生命周期（探活循环）
│   ├── server.py            # FastAPI 请求 handler + 鉴权中间件
│   ├── controller.py        # 请求编排 —— flash-first + 智能升级
│   ├── router.py            # 路由引擎 —— 三层分流决策
│   ├── config.py            # Pydantic v2 配置 + YAML 热加载
│   ├── state.py             # JSON 持久化状态管理（熔断/过载 TTL）
│   ├── circuit.py           # 三态熔断状态机 + 指数退避
│   ├── metrics.py           # LatencyTracker + SimilarityBuffer
│   ├── cache.py             # LRU + TTL 响应缓存
│   └── logging_setup.py     # 结构化 JSON Lines 日志（stdout + 文件）
│
├── logs/
│   ├── cc-switch.log        # Rust 主程序日志
│   └── smart_proxy.log      # smart_proxy 路由日志（JSON Lines）
│
├── backups/                 # 自动数据库备份
└── cc-switch.db             # SQLite 数据库（供应商/模型/用量）
```

---

## 3. 核心模块详解

### 3.1 路由引擎 (`router.py`)

路由决策分四步，按优先级依次判断：

```
请求进入
  │
  ├── 0. Token 过长（>100k）？
  │      └── ✅ 跳过 flash → _pick_best()
  │
  ├── 1. 会话粘性（同一会话复用上次后端）？
  │      └── ✅ 复用
  │
  ├── 2. 历史相似请求 flash 失败率 >30% 且样本 ≥3？
  │      └── ✅ 跳过 flash → _pick_best()
  │
  ├── 3. 关键词命中极高难度？
  │      └── ✅ 跳过 flash → _pick_best()
  │
  └── 4. 默认：flash_first
         └── ⚡ deepseek flash + 保留升级目标
```

**关键词检测 (`_skip_flash`)：** 只检查**最后一条** user 消息的文本，防止 Claude Code 系统 prompt 误触发。

**升级目标 (`_pick_best`)：**
```python
def _pick_best(settings):
    if "kimi" in settings.backends and _is_available("kimi"):
        return "kimi", "default"      # 链路 A: flash → kimi
    return "deepseek", "pro"          # 链路 B: flash → pro
```

### 3.2 请求编排 (`controller.py`)

执行 flash-first 策略后的响应评估：

```
flash 响应收到
  │
  ├── 流式（SSE）→ 直接转发，不评估
  │
  ├── 非流式：
  │     ├── stop_reason = max_tokens 且 output_tokens < 100
  │     │    → 截断，升级
  │     ├── stop_reason = tool_use
  │     │    → 工具调用，升级
  │     ├── 延迟 > 15s
  │     │    → 太慢，升级
  │     └── 其他 → 直接返回 ✅
  │
  └── flash 失败（HTTP 错误/网络异常）
       → 升级 + 熔断处理
```

**升级后如果还失败** → 再降级到任何可用后端（兜底机制）。

### 3.3 熔断系统 (`state.py` + `circuit.py`)

| 错误类型 | 条件 | 基础冷却 | 特性 |
|----------|------|----------|------|
| **quota** | 429 Too Many Requests / 403 billing 错误 | 3600s (1h) | 指数退避×2 |
| **server** | 5xx / 403（非 billing） | 300s (5min) | 指数退避×2 |
| **network** | 连接超时/断连 | 60s (1min) | 指数退避×2 |
| **permanent** | 403 + 关键词匹配（quota/billing）| 18000s (5h) | 一次性，不退避 |

**探活机制：** 后台每 30s 对熔断/过载的后端发 `{"messages":[{"role":"user","content":"hi"}],"max_tokens":1}`，成功后自动关闭断路器。

### 3.4 历史学习 (`metrics.py`)

**SimilarityBuffer** 基于 Jaccard 相似度（trigram 级）记录请求结果：

- 每次 flash 请求记录：文本特征、后端、tier、成功/失败、延迟、stop_reason
- 新请求进来时，对比历史相似请求的 flash 失败率
- 当相似请求 flash 失败率 >30% 且样本 ≥3，跳过 flash

**LatencyTracker** 滑动窗口（默认 8 个样本）记录延迟 + 错误率：

- `is_degraded()`: 平均延迟 >8s 或错误率 ≥50% 标记降级
- `mark_degraded()`: 错误发生后黏性标记 30s，避免短时反复重试

### 3.5 响应缓存 (`cache.py`)

- LRU 淘汰策略，最大 128 条目
- TTL 5 分钟
- 基于 `model + messages` 的 SHA256 摘要作为 key
- **仅缓存非流式请求**

### 3.6 日志与分析 (`logging_setup.py` + `analyze_routing.py`)

smart_proxy 输出 JSON Lines 结构化日志到两个目标：

| 目标 | 路径 | 格式 |
|------|------|------|
| stdout | 终端 | JSON Lines |
| 文件 | `logs/smart_proxy.log` | JSON Lines |

**analyze_routing.py** 解析两个日志源：

| 日志源 | 内容 | 用途 |
|--------|------|------|
| `logs/cc-switch.log` | 请求目标 URL、模型名、供应商切换事件 | 全量请求统计 |
| `logs/smart_proxy.log` | 路由决策（flash_first/skip_flash）、升级事件、错误 | 分流效果分析 |

运行生成交互式 HTML 报告，包含：KPI 概览、小时级分布图、路由分解饼图、供应商切换时间线、路由事件列表。

---

## 4. 两条链路详解

### 链路 A: kimi 可用

```
flash → 成功？→ flash 结果 ✅
      → 不足（截断/慢/工具调用）？→ kimi k2.7 → 结果 ✅
```

### 链路 B: kimi 熔断（额度耗尽）

```
flash → 成功？→ flash 结果 ✅
      → 不足？→ deepseek pro → 结果 ✅
```

**切换条件写在 config 里：**
```yaml
fallback_tier_map:
  "deepseek:pro": "kimi:default"
  "kimi:default": "deepseek:pro"
```

---

## 5. 关键配置参数

| 配置文件 | 参数 | 默认值 | 说明 |
|----------|------|--------|------|
| `proxy_config.yaml` | `routing.flash_first` | `true` | 是否启用 flash-first 策略 |
| | `routing.session_sticky.enabled` | `true` | 同一会话复用后端 |
| | `routing.token_length.threshold_tokens` | 100000 | 超长上下文跳过 flash |
| | `circuit.block_seconds_permanent` | 18000 | 永久错误熔断时长（5h）|
| | `latency.slow_threshold_ms` | 8000 | 慢查询阈值 |
| | `cache.ttl_seconds` | 300 | 响应缓存 TTL |
| `.env` | `DEEPSEEK_API_KEY` | — | DeepSeek API Key |
| | `MOONSHOT_API_KEY` | — | Kimi/Moonshot API Key |

---

## 6. 部署

### 启动方式

管理员权限运行 `start_proxy.bat`（需要管理员权限是因为 `taskkill` 需要 kill 已占用 8000 端口的旧进程）。

```
start_proxy.bat
    ├── 清理端口 8000（taskkill /f）
    ├── 激活 Anaconda build 环境
    ├── python -m smart_proxy
    └── └── uvicorn (127.0.0.1:8000)
```

### cc-switch 侧配置

在 cc-switch 中配置两个供应商用于热切换：
- **智能分流供应商**：目标 URL 设为 `http://127.0.0.1:8000/v1/messages`
- **直连供应商**：目标 URL 设为 `https://api.deepseek.com/anthropic/v1/messages`

---

## 7. 更新日志

| 版本 | 日期 | 变更 |
|------|------|------|
| v4.0.0 | 2026-07-24 | 初始架构文档 |
| | | 修复 `_extract_user_text` 误捡系统 prompt |
| | | 新增日志文件落盘 + 分析脚本 |

---

*此文档反映了当前的运行架构。所有配置通过 `proxy_config.yaml` 和 `.env` 管理，无需修改代码即可调整路由策略。*
