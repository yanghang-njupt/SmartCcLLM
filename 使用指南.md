# SmartProxy 智能分流系统 — 使用指南

> 面向使用者 · 版本 4.0.0 · 2026-07-24

---

## 目录

1. [这是什么](#1-这是什么)
2. [快速开始](#2-快速开始)
3. [两种使用模式](#3-两种使用模式)
4. [配置文件详解](#4-配置文件详解)
5. [日常运维](#5-日常运维)
6. [查看分流效果](#6-查看分流效果)
7. [故障排查](#7-故障排查)

---

## 1. 这是什么

SmartProxy 是一个**智能请求分流代理**，部署在你的 LLM 客户端和 API 提供商之间。它能自动判断每个请求的难度：

- **简单请求** → 发给便宜的 `deepseek-v4-flash`（省 token，省成本）
- **复杂请求** → 跳过 flash，直接发更强的模型
- **flash 响应不足**（截断、太慢） → 自动升级到 `deepseek-v4-pro` 或 `kimi-k2.7`

### 典型效果

| 场景 | 无分流器 | 有分流器 |
|------|----------|----------|
| 日常对话 | 每次都用 pro（贵） | flash 搞定，省 80%+ |
| 重构代码 | 用 pro 合理 | 自动识别 → 跳过 flash 直接 pro |
| kimi 额度用完 | 报错需要处理 | 自动降级到 deepseek pro，不中断 |

---

## 2. 快速开始

### 2.1 环境要求

- Windows 10/11
- Python 3.10+（推荐 Anaconda 环境）
- cc-switch 桌面应用（用于切换路由模式）
- DeepSeek API Key（必填）
- Kimi API Key（可选，没有则走 pro fallback）

### 2.2 安装步骤

**第一步：配置 API Key**

编辑根目录下的 `.env` 文件：

```ini
# DeepSeek API Key（必填）
DEEPSEEK_API_KEY=sk-your-deepseek-key

# Kimi / Moonshot API Key（可选）
MOONSHOT_API_KEY=sk-your-kimi-key
```

> API Key 获取地址：
> - DeepSeek: https://platform.deepseek.com/api_keys
> - Kimi: https://platform.moonshot.cn/console/api-keys

**第二步：启动代理**

**以管理员身份**运行 `start_proxy.bat`：

```
右键 start_proxy.bat → 以管理员身份运行
```

启动成功会看到：

```
SmartProxy v4.0 — Starting on 127.0.0.1:8000
INFO:     Started server process [xxxxx]
INFO:     Waiting for application startup.
INFO:     Application startup complete.
```

> ⚠️ 必须管理员权限，因为脚本需要先清理 8000 端口上的旧进程。

**第三步：配置 cc-switch**

在 cc-switch 中确保当前供应商指向 `http://127.0.0.1:8000/v1/messages`（详见下文"两种使用模式"）。

### 2.3 验证是否运行成功

打开浏览器访问：http://127.0.0.1:8000/health

```json
{
  "status": "ok",
  "backends": {
    "kimi": { "blocked": false, "latency": {...} },
    "deepseek": { "blocked": false, "latency": {...} }
  }
}
```

看到 `"status": "ok"` 即表示运行正常。

---

## 3. 两种使用模式

通过 cc-switch 桌面应用可以**热切换**两种模式，无需重启代理。

### 模式 A：智能分流（推荐）

请求路径：**Claude → cc-switch → SmartProxy → deepseek/kimi**

```
Claude Code 发请求
    ↓
cc-switch 路由到 localhost:8000
    ↓
SmartProxy 评估难度
    ├─ 简单 → deepseek flash（省钱）
    └─ 复杂 → deepseek pro / kimi（强模型）
```

在 cc-switch 中创建供应商，目标 URL 设为：
```
http://127.0.0.1:8000/v1/messages
```

### 模式 B：直连

请求路径：**Claude → cc-switch → api.deepseek.com**

```
Claude Code 发请求
    ↓
cc-switch 路由到 api.deepseek.com
    ↓
直接使用 deepseek-v4-pro（不走分流逻辑）
```

在 cc-switch 中创建供应商，目标 URL 设为：
```
https://api.deepseek.com/anthropic/v1/messages
```

### 切换方式

在 cc-switch 桌面应用中点击切换供应商即可，SmartProxy 无需重启。

---

## 4. 配置文件详解

### 4.1 路由配置 (`proxy_config.yaml`)

```yaml
server:
  host: 127.0.0.1
  port: 8000              # 监听端口

backends:
  kimi:                   # ★ Kimi 后端
    url: https://api.kimi.com/coding/v1/messages
    key_env: MOONSHOT_API_KEY
    models:
      default: k2.7

  deepseek:               # ★ DeepSeek 后端（flash + pro）
    url: https://api.deepseek.com/anthropic/v1/messages
    key_env: DEEPSEEK_API_KEY
    models:
      flash: deepseek-v4-flash     # 便宜模型（默认首选）
      pro: deepseek-v4-pro         # 强模型（升级目标/降级兜底）
      default: deepseek-v4-pro

routing:
  flash_first: true                # ★ 核心策略：开启 flash-first
  session_sticky:
    enabled: true                  # 同一会话复用上次后端
    ttl_seconds: 600
  token_length:
    enabled: true
    threshold_tokens: 100000       # 超长上下文跳过 flash
```

### 4.2 关键参数调优

| 参数 | 默认值 | 说明 | 调优建议 |
|------|--------|------|----------|
| `latency.slow_threshold_ms` | 8000 | flash 超过此时长触发升级 | 网络好可降到 5000 |
| `latency.window` | 8 | 延迟统计滑动窗口大小 | 越大越平滑，越小越敏感 |
| `circuit.block_seconds_permanent` | 18000 | 配额耗尽熔断时长（5h） | 一般不需要改 |
| `circuit.probe_interval` | 30 | 探活间隔（秒） | 调小可更快恢复熔断 |
| `cache.ttl_seconds` | 300 | 响应缓存 TTL | 调大省请求，调小保新鲜 |

---

## 5. 日常运维

### 5.1 启动 / 重启

```bash
# 管理员运行
start_proxy.bat
```

脚本会自动：
1. 检查并清理 8000 端口
2. 激活 Python 环境
3. 启动 uvicorn 服务

### 5.2 关闭

直接关闭启动脚本的 cmd 窗口即可。

### 5.3 熔断状态查看

健康检查接口可以看到所有后端状态：

```bash
curl http://127.0.0.1:8000/health
```

重点关注：
- `"blocked": true` — 该后端被熔断（自动恢复）
- `"overloaded": true` — 该后端被限流（自动恢复）
- `"error_rate": 1.0` — 全部请求失败

### 5.4 手动解除熔断

如果确认后端已恢复，重启 SmartProxy 即可清零熔断状态。

---

## 6. 查看分流效果

### 6.1 生成可视化报告

```bash
python analyze_routing.py [日期]
# 示例：查看今天的数据（默认）
python analyze_routing.py
# 示例：查看指定日期
python analyze_routing.py 2026-07-24
```

运行后在浏览器打开 `routing_report_2026-07-24.html`。

### 6.2 报告内容解读

报告包含：

| 指标 | 说明 |
|------|------|
| **flash 直接返回** | 用 flash 模型成功响应，未触发升级 → 对用户请求来说 flash 足够 |
| **flash 升级** | flash 响应不足（截断/太慢），自动升级到更强模型 |
| **跳过 flash** | 系统识别为复杂任务，直接使用强模型 |
| **小时分布** | 不同时间段直连 vs 经分流器的请求量对比 |

**健康指标：**

| 指标 | 正常 | 预警 |
|------|------|------|
| 升级率（upgraded / total） | <20% | >50%（说明 flash 质量不够，需排查）|
| 错误率 | 0% | >5%（检查 API Key 或网络）|

---

## 7. 故障排查

### 7.1 启动报 "Cannot kill process"

**现象：** 日志输出 `[WARN] Cannot kill process XXXX, starting anyway...`

**原因：** 没有以管理员身份运行 bat 脚本。

**解决：** 关闭窗口，右键 → "以管理员身份运行"。

### 7.2 所有请求都 "skip_flash"

**现象：** 报告显示 0% flash 直接返回

**原因：** 修复前 Bug — `_extract_user_text()` 误检了系统 prompt 中的关键词。

**解决：** 确认已更新到最新代码（`router.py` 中 `_extract_user_text` 只检查最后一条 user 消息）。

### 7.3 请求报 502 "All backends failed"

**现象：** 所有后端都返回错误

**排查步骤：**
1. 检查健康接口 `http://127.0.0.1:8000/health` 查看各后端状态
2. 检查 `.env` 中的 API Key 是否有效
3. 检查 `logs/smart_proxy.log` 中的错误详情

### 7.4 kimi 一直熔断

**现象：** 健康接口显示 kimi 为 blocked

**原因：** kimi 账号额度用完，自动熔断 5 小时。

**解决：**
- 等待自动恢复（探活机制每 30s 检查一次）
- 或者为 kimi 账号充值
- 不需要手动操作，系统会自动降级到 deepseek pro

---

## 附录：文件清单

| 文件 | 用途 | 需要修改？ |
|------|------|-----------|
| `.env` | API Key 配置 | ✅ 首次使用必改 |
| `proxy_config.yaml` | 路由/熔断/缓存参数 | ⬜ 按需调整 |
| `start_proxy.bat` | 启动脚本 | ❌ |
| `analyze_routing.py` | 日志可视化分析 | ❌ |
| `logs/smart_proxy.log` | 路由决策日志 | 自动生成 |
| `proxy_state.json` | 熔断持久化状态 | 自动生成 |
| `smart_proxy/` | Python 源码包 | ❌ 通常不动 |

---

*如遇到其他问题，查看 `logs/smart_proxy.log` 或 `logs/cc-switch.log` 获取详细错误日志。*
